<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Profile | Digital Voting Portal</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    
    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      min-height: 100vh;
      padding: 40px 20px;
      display: flex;
      justify-content: center;
    }
    
    .container {
      width: 100%;
      max-width: 800px;
    }
    
    .profile-card {
      background-color: white;
      border-radius: 12px;
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      margin-bottom: 30px;
      animation: fadeIn 0.6s ease forwards;
    }
    
    .profile-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 25px 30px;
      position: relative;
    }
    
    .profile-header h1 {
      font-size: 28px;
      font-weight: 600;
      margin-bottom: 5px;
    }
    
    .profile-badge {
      display: inline-block;
      background-color: rgba(255, 255, 255, 0.2);
      padding: 6px 12px;
      border-radius: 20px;
      font-size: 14px;
      margin-top: 5px;
    }
    
    .profile-badge svg {
      width: 16px;
      height: 16px;
      vertical-align: middle;
      margin-right: 5px;
    }
    
    .avatar {
      position: absolute;
      right: 30px;
      top: 50%;
      transform: translateY(-50%);
      width: 80px;
      height: 80px;
      background-color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    }
    
    .avatar svg {
      width: 50px;
      height: 50px;
      color: #667eea;
    }
    
    .profile-content {
      padding: 30px;
    }
    
    .profile-section {
      margin-bottom: 10px;
    }
    
    .field-label {
      font-size: 14px;
      color: #777;
      margin-bottom: 5px;
    }
    
    .field-value {
      font-size: 16px;
      color: #333;
      background-color: #f8f9fa;
      padding: 12px 15px;
      border-radius: 6px;
      border-left: 3px solid #667eea;
      margin-bottom: 15px;
      word-break: break-word;
    }
    
    .voting-card {
      background-color: white;
      border-radius: 12px;
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
      padding: 30px;
      text-align: center;
      animation: fadeIn 0.6s ease forwards 0.2s;
      opacity: 0;
    }
    
    .voting-info {
      margin-bottom: 25px;
    }
    
    .voting-info h2 {
      font-size: 22px;
      color: #333;
      margin-bottom: 10px;
    }
    
    .voting-info p {
      color: #666;
      line-height: 1.6;
    }
    
    .vote-button {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      padding: 14px 35px;
      border-radius: 6px;
      font-size: 16px;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    
    .vote-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 7px 14px rgba(102, 126, 234, 0.4);
    }
    
    .vote-button:active {
      transform: translateY(0);
    }
    
    /* Animation */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    
    @media (max-width: 600px) {
      .avatar {
        position: relative;
        right: auto;
        top: auto;
        transform: none;
        margin: 0 auto 20px;
      }
      
      .profile-header {
        text-align: center;
        padding-bottom: 30px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="profile-card">
      <div class="profile-header">
        <h1>User Profile</h1>
        <div class="profile-badge">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
            <polyline points="22 4 12 14.01 9 11.01"></polyline>
          </svg>
          Verified Voter
        </div>
        <div class="avatar">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
            <circle cx="12" cy="7" r="4"></circle>
          </svg>
        </div>
      </div>
      
      <div class="profile-content">
        <div class="profile-section">
          <div class="field-label">Full Name</div>
          <div class="field-value"><%=userdata.name%></div>
        </div>
        
        <div class="profile-section">
          <div class="field-label">Age</div>
          <div class="field-value"><%=userdata.age%></div>
        </div>
        
        <div class="profile-section">
          <div class="field-label">Address</div>
          <div class="field-value"><%=userdata.address%></div>
        </div>
        
        <div class="profile-section">
          <div class="field-label">Aadhaar Number</div>
          <div class="field-value"><%=userdata.adharNumber%></div>
        </div>
        
        <div class="profile-section">
          <div class="field-label">Email Address</div>
          <div class="field-value"><%=userdata.email%></div>
        </div>
      </div>
    </div>
    
    <div class="voting-card">
      <div class="voting-info">
        <h2>Ready to Cast Your Vote?</h2>
        <p>Please verify your information above before proceeding to the ballot. Your vote is confidential and secure.</p>
      </div>
      <form action="/candidate/vote">
        <button type="submit" class="vote-button">
          Cast My Vote
        </button>
      </form>
    </div>
  </div>
</body>
</html>